#!/usr/bin/env bash
echo "lyra-assistant: Set LYRA_DIR env var to your assistant project path"
if [[ -n "$LYRA_DIR" && -f "$LYRA_DIR/src/cli.py" ]]; then
  exec "$LYRA_DIR/.venv/bin/python3" "$LYRA_DIR/src/cli.py"
else
  echo "Usage: LYRA_DIR=/path/to/assistant lyra-assistant"
  exit 1
fi
